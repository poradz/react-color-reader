// handleSelection () {
    // let textVal = this.MyColorInst; 
    // let cursorStart = textVal.selectionStart;
    // let cursorEnd = textVal.selectionEnd;
    // let selected = this.state.inputVal.substring(cursorStart,cursorEnd) 
    // console.log(selected);
    // $(document).on('keydown', function (event) {
    // if (event.which == 38 || event.which == 104) {
    //     $input.val((parseInt($input.val()) + 1));
    // } else if (event.which == 40 || event.which == 98) {
    //     $input.val((parseInt($input.val()) - 1));
    // }
    //   });
  //   let textVal = this.refs.myTextarea; 

  //   if (this.state.textareaVal !== '') {    
  //   let textVal = this.refs.myTextarea; 
  //   let cursorStart = textVal.selectionStart;
  //   let cursorEnd = textVal.selectionEnd;
  //   let selected = this.state.textareaVal.substring(cursorStart,cursorEnd)
  //   console.log(selected);
  //   console.log(cursorStart);
  //    console.log(cursorEnd);
  //   return selected;
  // }              
    //       let cursorStart = textVal.selectionStart;
    //       let cursorEnd = textVal.selectionEnd;

    //   if (cursorStart != cursorEnd) {

    //       var newText = this.state.textareaVal.substring (0, cursorStart) + 
    //           "[start]" + this.state.textareaVal.substring  (cursorStart, cursorEnd) + "[end]" +
    //           this.state.textareaVal.substring (cursorEnd);
    //       this.state.textareaVal = newText;

    //       return newText;
    //   }
    // }
  // }

 // onSelectionChange (event) { 
 //   if (this.state.selectionState) { 
 //     var selection = event.nativeEvent.selection; 
 //     this.state.selectionState.update(selection.start, selection.end); 
 //   } 
 //   this.state.onSelectionChange && this.state.onSelectionChange(event); 
 //  }; 

       <textarea
       //     className='html-editor'  
       //     ref='myTextarea' 
       //    value = {this.state.textareaVal}
       //    // onChange={(event)=>{
       //    //             this.setState({
       //    //                textareaVal: event.target.value
       //    //             });
       //    //          }}
       //    onSelectionChange = {this.onSelectionChange}
        >
      </textarea>